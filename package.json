{
  "name": "spreadsheets-form-server",
  "version": "1.0.0",
  "description": "A simple Express server to proxy requests to Google Apps Script and avoid CORS issues",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "test": "echo 'No tests configured yet'",
    "deploy": "echo 'Deploy to your preferred platform (Railway, Render, Heroku, etc.)'"
  },
  "keywords": [
    "express",
    "google-sheets",
    "cors",
    "proxy"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0",
    "helmet": "^7.1.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}